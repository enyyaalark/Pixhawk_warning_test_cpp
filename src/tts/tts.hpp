#pragma once

/// TTS (Text-to-Speech) module — placeholder stub matching the Python version.
///
/// When implemented, this module will convert warning events into local
/// voice output (e.g., via espeak system calls).  For now, it exports an
/// empty interface.

namespace tts {

// Future: void speak(const warning::WarningEvent& event);

}  // namespace tts
